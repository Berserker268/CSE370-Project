<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
    body { height: 100vh; display: flex; background: #f0f2f5; }

    .left-side {
      width: 33.33%;
      background: #fff;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 40px;
      box-shadow: 2px 0 10px rgba(0,0,0,0.1);
    }

    .left-side h1 { margin-bottom: 25px; font-size: 2rem; color: #333; }

    .form-container { width: 100%; }
    .form-container form { display: flex; flex-direction: column; gap: 15px; }
    label { font-weight: 600; margin-bottom: 5px; display: block; }
    input { padding: 10px 12px; border-radius: 8px; border: 1px solid #ccc; font-size: 1rem; transition: border 0.2s; }
    input:focus { outline: none; border-color: #2575fc; box-shadow: 0 0 5px rgba(37,117,252,0.5); }

    button { padding: 12px; border: none; border-radius: 8px; background: #2575fc; color: #fff; font-size: 1rem; cursor: pointer; transition: background 0.3s; }
    button:hover { background: #6a11cb; }

    .link { margin-top: 15px; display: block; font-size: 0.95rem; color: #2575fc; text-decoration: none; transition: color 0.3s; }
    .link:hover { color: #6a11cb; }

    .error-message { color: #e74c3c; margin-bottom: 10px; }

    .right-side {
      width: 66.66%;
      background: url('https://images.unsplash.com/photo-1591696331119-11b2d27a735d?auto=format&fit=crop&w=1350&q=80') no-repeat center center;
      background-size: cover;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .right-side h2 {
      color: white;
      font-size: 2.5rem;
      text-shadow: 2px 2px 10px rgba(0,0,0,0.7);
      max-width: 500px;
      text-align: center;
    }

    @media (max-width: 900px) {
      body { flex-direction: column; }
      .left-side, .right-side { width: 100%; height: 50vh; }
      .right-side h2 { font-size: 2rem; }
    }
  </style>
</head>
<body>
  <div class="left-side">
    <h1>Register</h1>

    <% if (messages.error) { %>
      <div class="error-message"><%= messages.error %></div>
    <% } %>

    <div class="form-container">
      <form action="/register" method="POST">
        <div>
          <label for="name">Name</label>
          <input type="text" id="name" name="name" placeholder="Your full name" required>
        </div>
        <div>
          <label for="email">Email</label>
          <input type="email" id="email" name="email" placeholder="you@example.com" required>
        </div>
        <div>
          <label for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Enter a password" required>
        </div>
        <button type="submit">Register</button>
      </form>

      <a class="link" href="/login">Already have an account? Login</a>
    </div>
  </div>

  <div class="right-side">
    <h2>Start sharing and organizing your notes with friends and colleagues!</h2>
  </div>
</body>
</html>
